const angular      = require('angular'),

      ngModuleName = 'template',
      ngModule     = angular.module(ngModuleName, []);

// ngModule.service(…)
// ngModule.directive(…)
// etc.

module.export = ngModuleName;
